> ⚠️ **历史文档**：本文档记录了设计演化过程（M1 的 H0-H4 与反射层已被废弃，
> H/E 家族与"Tier B"也已废弃）。
> 当前权威设计与实现见 [PROPOSAL.md](PROPOSAL.md)（端点即数据范式）与 README。

# 非 Gin 风格、泛型 + 显式优先的 Go Web 框架设计（历史记录）

> 目标：一个性能不低于 Gin、API 设计更合理、使用更方便的 Web 框架。
> 三个关键词：**泛型优先（typed over `any`）**、**显式优先（explicit over magic）**、**注册期做重活、运行期零反射**。
>
> 本文件保留自设计早期阶段（M1 内核 + Tier A/B 讨论）。当前框架采用
> 端点即数据范式（Tapir 路线），详见 PROPOSAL.md；两者共享的核心原则
> 是 §1.1 的"Context 职责半径 = 框架风味"与错误即值、响应即值的控制流模型。
